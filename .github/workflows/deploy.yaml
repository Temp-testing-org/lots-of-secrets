name: Python stuff
on:
  push:
    branches: [ "none" ]
  pull_request:
    branches: [ "none" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: lint
        uses: psf/black@stable
        with:
          options: "--check --verbose"
          src: "./src"
