name: Python stuff
on:
  push:
    branches: [ "main", "eriks-branch" ]
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ORG_SECRET: ${{ secrets.ORGANIZATION_SECRET }}
    steps:
      - name: checkout
        uses: actions/checkout@v4


      - name: Run a one-line script
        run: |
          echo "$ENV_SECRET"
          echo "ENV_SECRET: $(base64 <<< $(base64 <<< "$ENV_SECRET"))"
